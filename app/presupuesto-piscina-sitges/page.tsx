import Link from "next/link";import { Ctas, JsonLd, ServiceGrid } from "@/components/ui";import { ContactForm } from "@/components/contact-form";import { breadcrumb, faqSchema, faqsConstruccion, faqsReforma, meta, serviceSchema } from "@/lib/page-helpers";import { zones } from "@/lib/site-data";
export const metadata=meta("/presupuesto-piscina-sitges");
export default function Page(){const path="/presupuesto-piscina-sitges";const title="Presupuesto para piscina en Sitges";const desc="Solicita presupuesto para construir o reformar una piscina en Sitges. Valoración personalizada según estado, acabados, accesos y entorno.";let qs: string[] = []; if (path.includes("construccion")) qs = faqsConstruccion; if (path.includes("reforma-piscinas-sitges")) qs = faqsReforma;return <div className="page"><JsonLd data={breadcrumb(path,title)}/><JsonLd data={serviceSchema(title,path,desc)}/>{qs.length>0&&<JsonLd data={faqSchema(qs)}/>}<p className="breadcrumbs"><Link href="/">Inicio</Link> · {title}</p><p className="eyebrow">Piscines Aiguadolç · Sitges i rodalies</p><h1>{title}</h1><p className="lead">Solicita presupuesto para construir o reformar una piscina en Sitges. Valoración personalizada según estado, acabados, accesos y entorno.</p><Ctas/><section className="split"><div><h2>Qué influye en el presupuesto</h2><p>Construcción o reforma, estado actual, dimensiones, acabados, entorno, accesos, tipo de intervención y nivel de personalización condicionan la propuesta.</p><h2>Solicita una valoración</h2><p>Cuanto más claro sea el punto de partida, mejor podremos orientar la intervención sin dar rangos genéricos ni precios poco útiles.</p></div><aside className="card"><span>Valoración personalizada</span><p>Explícanos la zona, el estado actual, el tipo de intervención y el plazo aproximado. Te orientaremos con una propuesta ajustada al proyecto.</p><Link href="/presupuesto-piscina-sitges" className="btn">Valorar mi piscina</Link></aside></section><section className="band"><h2>Servicios relacionados</h2><ServiceGrid/></section><section><h2>Zonas de servicio en Sitges y Costa del Garraf</h2><p>Atención y valoraciones para propiedades en {zones.join(", ")}.</p><div className="chips">{zones.map(z=><span className="chip" key={z}>{z}</span>)}</div></section>{qs.length>0&&<section className="faq"><h2>Preguntas frecuentes sobre {title.toLowerCase()}</h2>{qs.map(q=><details key={q}><summary>{q}</summary><p>Revisamos el caso, el acceso, el entorno y las necesidades antes de orientar una solución o presupuesto.</p></details>)}</section>}<ContactForm/><Ctas/></div>}
