import { Ctas, JsonLd } from "@/components/ui";import { ContactForm } from "@/components/contact-form";import { contactConfig, whatsappHref } from "@/lib/contact-config";import { breadcrumb, meta } from "@/lib/page-helpers";
export const metadata=meta("/contacto");
export default function Page(){return <div className="page"><JsonLd data={breadcrumb("/contacto","Contacto")}/><p className="eyebrow">Contacto directo</p><h1>Contacta para construir o reformar tu piscina en Sitges</h1><p className="lead">Cuéntanos si buscas construcción, reforma integral, renovación de acabados o diseño exterior con piscina en Sitges y Costa del Garraf.</p><div className="split"><div className="card"><span>Datos de contacto</span><p><b>Teléfono:</b> <a href={contactConfig.phoneHref}>{contactConfig.phoneDisplay}</a></p><p><b>WhatsApp:</b> <a href={whatsappHref()}>{contactConfig.whatsappNumber}</a></p><p><b>Email:</b> <a href={`mailto:${contactConfig.email}`}>{contactConfig.email}</a></p><p><b>Zona:</b> {contactConfig.serviceArea}</p><Ctas/></div><ContactForm/></div></div>}
