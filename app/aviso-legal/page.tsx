import { meta } from "@/lib/page-helpers";import { contactConfig } from "@/lib/contact-config";export const metadata=meta("/aviso-legal");export default function Page(){return <div className="page legal"><h1>Aviso legal</h1><p>Este sitio web pertenece a {contactConfig.companyName} y presenta servicios de construcción, reforma y renovación de piscinas en {contactConfig.serviceArea}.</p><p>El contenido tiene finalidad informativa y comercial. Para cualquier consulta legal o de contacto puedes escribir a <a href={`mailto:${contactConfig.email}`}>{contactConfig.email}</a> o llamar al <a href={contactConfig.phoneHref}>{contactConfig.phoneDisplay}</a>.</p></div>}
