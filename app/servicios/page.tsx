import Link from "next/link";import { Ctas, JsonLd, ServiceGrid } from "@/components/ui";import { breadcrumb, faqSchema, faqsConstruccion, faqsReforma, meta, serviceSchema } from "@/lib/page-helpers";import { zones } from "@/lib/site-data";
export const metadata=meta("/servicios");
export default function Page(){const path="/servicios";const title="Servicios de construcción y reforma de piscinas";const desc="Construcción de piscinas, reforma integral, renovación de acabados y diseño exterior en Sitges y Costa del Garraf.";let qs: string[] = []; if (path.includes("construccion")) qs = faqsConstruccion; if (path.includes("reforma-piscinas-sitges")) qs = faqsReforma;return <div className="page"><JsonLd data={breadcrumb(path,title)}/><JsonLd data={serviceSchema(title,path,desc)}/>{qs.length>0&&<JsonLd data={faqSchema(qs)}/>}<p className="breadcrumbs"><Link href="/">Inicio</Link> · {title}</p><p className="eyebrow">Piscines Aiguadolç · Sitges i rodalies</p><h1>{title}</h1><p className="lead">Construcción de piscinas, reforma integral, renovación de acabados y diseño exterior en Sitges y Costa del Garraf.</p><Ctas/><section className="split"><div><h2>Un hub claro para decidir la intervención</h2><p>Organizamos cada solicitud según objetivo: construir una piscina de obra, renovar una piscina antigua, actualizar acabados o rediseñar la terraza con piscina.</p><h2>Enlaces útiles para avanzar</h2><p>Desde esta página puedes entrar en cada servicio, pedir presupuesto, consultar zonas y contactar por WhatsApp.</p></div><aside className="card"><span>Valoración personalizada</span><p>Explícanos la zona, el estado actual, el tipo de intervención y el plazo aproximado. Te orientaremos con una propuesta ajustada al proyecto.</p><Link href="/presupuesto-piscina-sitges" className="btn">Valorar mi piscina</Link></aside></section><section className="band"><h2>Servicios relacionados</h2><ServiceGrid/></section><section><h2>Zonas de servicio en Sitges y Costa del Garraf</h2><p>Atención y valoraciones para propiedades en {zones.join(", ")}.</p><div className="chips">{zones.map(z=><span className="chip" key={z}>{z}</span>)}</div></section>{qs.length>0&&<section className="faq"><h2>Preguntas frecuentes sobre {title.toLowerCase()}</h2>{qs.map(q=><details key={q}><summary>{q}</summary><p>Revisamos el caso, el acceso, el entorno y las necesidades antes de orientar una solución o presupuesto.</p></details>)}</section>}<Ctas/></div>}
